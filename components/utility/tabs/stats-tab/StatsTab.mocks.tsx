import { IUserData } from '../../../../data/types';
import { IStatsTab } from './StatsTab';

const base: IStatsTab = {
  midwifeOfInterest: {} as IUserData,
};

export const mockStatsTabProps = {
  base,
};
